# Rust build artifacts — regenerated by `cargo build` in the multi-stage
# Dockerfile and by `cargo test` locally. Never commit (arch-specific,
# tens of MB). Cargo.lock IS committed on purpose: envtoxml is a binary,
# and pinning its one dependency (quick-xml) keeps the image build
# reproducible.
/target
